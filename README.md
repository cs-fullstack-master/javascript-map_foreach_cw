# javascript-map_foreach_cw

### Problem 1
Create a function that will take the array ["Kenn", "Kevin", "Autumn", "Erin"] as a parameter, and return the array with "Hello" at the beginning of each string.

### Problem 2
Ask the user to enter a word. Keep asking them to enter a word until they enter the letter 'q'.

### Problem 3
Create a function that will take a string array and return that string array with all lower case strings. If you don't remember how to use that Google it.

### Problem 4
Create a function that will take a string array as a parameter and return an array of objects of the specified class below

```
class Student{
        constructor(name){
                this.name = name;
        }
}
```
